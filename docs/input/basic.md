---
order: 0
title:
  zh-CN: 基本
  en-US: Basic
subtitle: 
  zh-CN: 基本用法，可以用`type`、 `round` 和 `circle`来定义Input的样式。
  en-US: Use `type`, `round` and `circle` to define Input's style.
---

```js
import { Input } from 'dashkit-ui';

ReactDOM.render(
  <Input placeholder="This is a normal Input" defaultValue="Hello." />,
  mountNode
);
```