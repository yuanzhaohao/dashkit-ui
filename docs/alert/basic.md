---
order: 0
title:
  zh-CN: 基本
  en-US: Basic
subtitle: 
  zh-CN: 基本用法，可以用`type`、 `round` 和 `circle`来定义Alert的样式。
  en-US: Use `type`, `round` and `circle` to define the style of Alert.
---

```js
import { Alert } from 'dashkit-ui';

ReactDOM.render(
  <div className="alert-wrapper">
    <Alert />
  </div>,
  mountNode
);
```