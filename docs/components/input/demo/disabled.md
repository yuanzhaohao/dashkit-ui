---
order: 2
title:
  zh-CN: 禁用
  en-US: Disabled
subtitle:
  zh-CN: 使用`disabled`来决定是否禁用输入框，默认值是`false`。
  en-US: Add the `disabled` property to determine if the input is disabled.
---

```js
import { Input } from 'dashkit-ui';

ReactDOM.render(<Input placeholder="This is a diabled Input" disabled={true} />, mountNode);
```
