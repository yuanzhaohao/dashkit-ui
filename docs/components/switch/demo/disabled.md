---
order: 2
title:
  zh-CN: 禁用
  en-US: Disabled
subtitle:
  zh-CN: 使用`disabled`来决定是否禁用switch，默认值是`false`。
  en-US: Add the `disabled` property to determine if the switch is disabled.
---

```js
import { Switch } from 'dashkit-ui';

ReactDOM.render(<Switch checked={true} disabled={true} />, mountNode);
```
